{
  "name": "liyog-push-notify",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-vapid-keys": "node scripts/generate-vapid-keys.mjs",
    "db:migrate": "wrangler d1 execute liyog-notify-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
